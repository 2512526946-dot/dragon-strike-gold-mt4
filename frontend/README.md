# 巨龙出击 Frontend

前端当前为 0C-1 静态首页预览，只展示项目阶段、安全状态和交易约束说明。

本轮不请求后端接口，不接入实时行情，不展示图表，不返回真实交易建议。

## 启动

```powershell
cd frontend
npm install
npm run dev
```

## 构建

```powershell
cd frontend
npm run build
```
